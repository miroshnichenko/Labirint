<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <appSettings>
    <!-- Адрес сервера (например, localhost или 127.0.0.1) -->
    <add key="server" value="M-PC" />
    <!-- Название БД (например, Labirint) -->
    <add key="db" value="Labirint" />
    <!-- Количество номерков (от 20 до 100) -->
    <add key="tokens" value="100" />
  </appSettings>
</configuration>